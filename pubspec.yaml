name: kaunti47_v2
description: "Kaunti47 v2 - Kenya county-badging travel app."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: ^3.11.4

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8
  supabase_flutter: ^2.17.2
  google_sign_in: ^7.2.0
  sign_in_with_apple: ^7.0.1
  crypto: ^3.0.7
  flutter_svg: ^2.2.4
  permission_handler: ^13.0.1
  url_launcher: ^6.3.2
  # Routing: start-up gating as redirects, tabs as a shell route, detail
  # pages as real routes (docs/architecture.md §5). ^17: 18 needs Dart 3.12.
  go_router: ^17.0.0

  # Home's default map (real map); the drawn county map is the fallback
  # when Mapbox can't load. Token: MAPBOX_ACCESS_TOKEN dart-define.
  mapbox_maps_flutter: ^2.31.0

  # State management: Riverpod ONLY, providers declared with @riverpod
  # codegen (see docs/architecture.md). Enforced by
  # tools/check_architecture.py and riverpod_lint.
  flutter_riverpod: ^3.0.0
  riverpod_annotation: ^3.0.0

  # Detection's on-device visit queue (doc 01: a crossing is recorded
  # locally first and synced when online). drift_flutter brings sqlite in
  # transitively; don't pin sqlite3_flutter_libs (deprecated, it caused a
  # solver conflict in v1).
  # Held on 2.31 with drift_dev (see dev_dependencies).
  drift: ">=2.31.0 <2.32.0"
  # 0.2.x: 0.3 needs sqlite3 3.x, which drift_dev 2.31 (see below) rejects.
  drift_flutter: ^0.2.8

  # The arrival sheet's "already shown" history (one entry per crossing),
  # under v1's key so an upgraded phone doesn't re-show old crossings.
  shared_preferences: ^2.5.5

  # Detection: OS-level geofencing (Android GeofencingClient, iOS region
  # monitoring) -- not continuous GPS (doc 01).
  native_geofence: ^1.3.1

  # Opt-in Journey route capture while the app process is alive. County
  # detection continues to use native_geofence, not this continuous stream.
  location: ^10.0.2


dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0

  # Riverpod codegen + lints.
  build_runner: ^2.4.0
  # drift's generated tables (part '*.g.dart'). Held on 2.31: it's the last
  # drift_dev that accepts analyzer 8, which custom_lint 0.8.1 (riverpod_lint
  # 3.0) and riverpod_generator 3.x need; drift_dev >=2.32 needs analyzer
  # 10+. Lift both together when riverpod_lint moves to 3.1+ (analysis
  # server plugin, no custom_lint) on a Dart SDK that supports it.
  drift_dev: ">=2.31.0 <2.32.0"
  riverpod_generator: ^3.0.0
  custom_lint: ^0.8.0
  riverpod_lint: ^3.0.0

dependency_overrides:
  permission_handler_android: 13.0.1

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  assets:
    - assets/images/
    - assets/geo/

  fonts:
    - family: Inter
      fonts:
        - asset: assets/fonts/Inter-Regular.ttf
          weight: 400
        - asset: assets/fonts/Inter-Medium.ttf
          weight: 500
        - asset: assets/fonts/Inter-SemiBold.ttf
          weight: 600

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
